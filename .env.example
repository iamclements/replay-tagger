# Copy this file to .env and fill in your values.
# .env is listed in .gitignore; NEVER commit secrets.

# ── User / group identity ──────────────────────────────────────────────────────
# Set to the UID/GID that owns your clips folder on the host so the container
# can read and write files without permission errors.
# Run `id` on your server to find your UID and GID.
PUID=1000
PGID=1000

# ── Clips directory ────────────────────────────────────────────────────────────
# Absolute path to your game clips folder (used by docker-compose)
CLIPS_DIR=/path/to/your/game/clips

# ── Plex ──────────────────────────────────────────────────────────────────────
# Get your token from Plex Web: browse to any item → (...) → Get Info → View XML
# Copy X-Plex-Token from the URL bar. Tokens don't expire.
PLEX_TOKEN=your_plex_token_here
PLEX_URL=http://192.168.1.100:32400

# Enable Plex integration (required if not using config.yaml)
PLEX_ENABLED=true

# Name of your game clips library in Plex (must match exactly, case-sensitive)
PLEX_LIBRARY_NAME=Gaming Clips

# Uncomment if your Plex server uses HTTPS with a self-signed cert (common on LAN)
# PLEX_VERIFY_SSL=false

# Uncomment to disable automatic Plex scan or collection creation
# PLEX_AUTO_SCAN=true
# PLEX_AUTO_COLLECTIONS=true

# ── SteamGridDB (optional) ────────────────────────────────────────────────────
# When set, ReplayTagger fetches portrait artwork from SteamGridDB and uploads
# it to Plex when a new collection is created. Covers Steam, Ubisoft Connect,
# Battle.net, and community-submitted art for modded/private clients.
# Get a free API key at: https://www.steamgriddb.com/profile/preferences/api
# STEAMGRIDDB_API_KEY=your_api_key_here

# ── YouTube ───────────────────────────────────────────────────────────────────
# Uncomment to enable YouTube uploads without editing config.yaml
# YOUTUBE_ENABLED=true

# Automatically upload new clips as they arrive in watch mode
# YOUTUBE_AUTO_UPLOAD=true

# OAuth client credentials from Google Cloud Console (TV and Limited Input Devices type).
# Use these instead of mounting a credentials JSON file (recommended for Docker deployments).
# YOUTUBE_CLIENT_ID=your_client_id_here
# YOUTUBE_CLIENT_SECRET=your_client_secret_here

# Override the privacy setting from config.yaml (private | unlisted | public)
YOUTUBE_PRIVACY=private

# Uncomment to delay auto-uploads until clips have been in the folder for N days
# (gives time to trim and rename before they appear on YouTube)
# YOUTUBE_UPLOAD_AFTER_DAYS=3

# YouTube category ID for uploaded clips. 20 = Gaming (default).
# Full list: https://developers.google.com/youtube/v3/docs/videoCategories/list
# YOUTUBE_CATEGORY_ID=20

# Tags applied to every uploaded clip (comma-separated), alongside the game name.
# YOUTUBE_TAGS=gaming,clips

# Hour (0-23, local time) at which the watch command runs a daily YouTube sync pass.
# Defaults to 3 (3am). This automatic pass retries any clips skipped due to quota limits,
# so uploads resume the morning after a quota reset without manual intervention.
# YOUTUBE_SYNC_HOUR=3

# ── Game name mapping (optional) ─────────────────────────────────────────────
# Map clip folder names to canonical game names without editing config.yaml.
# Must be valid JSON. Env var entries take precedence over config.yaml entries.
# GAME_NAME_MAP={"Apex Legends Season 20": "Apex Legends", "Call of Duty HQ": "Call of Duty: Warzone"}

# ── Watcher ───────────────────────────────────────────────────────────────────
# Seconds to wait after a file event before processing (lets Syncthing finish writing)
# DEBOUNCE_SECONDS=10

# Optional: directory for ffmpeg temp files. Set to /app/data to keep temp files
# out of the synced clips folder (prevents EBUSY errors when using Syncthing).
# FFMPEG_TEMP_DIR=/app/data

# ── Notifications (optional) ──────────────────────────────────────────────────
# Set WEBHOOK_URL to enable a single webhook without editing config.yaml.
# WEBHOOK_TYPE defaults to "discord" if the URL contains discord.com, otherwise "generic".
# WEBHOOK_EVENTS is a comma-separated list of events to receive (default: scan_complete,error).
# Supported events: scan_complete, error, clip_tagged, clip_uploaded
# For multiple webhooks or per-webhook event filtering, use config.yaml instead.
# WEBHOOK_URL=https://discord.com/api/webhooks/SERVER_ID/TOKEN
# WEBHOOK_TYPE=discord
# WEBHOOK_EVENTS=scan_complete,error

# ── Logging ───────────────────────────────────────────────────────────────────
# Use "text" for human-readable output, "json" for structured production logs
LOG_LEVEL=INFO
LOG_FORMAT=json
